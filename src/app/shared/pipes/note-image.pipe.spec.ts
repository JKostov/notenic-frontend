import { NoteImagePipe } from './note-image.pipe';

describe('NoteImagePipe', () => {
  it('create an instance', () => {
    const pipe = new NoteImagePipe();
    expect(pipe).toBeTruthy();
  });
});
